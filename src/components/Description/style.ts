import styled from "styled-components";

const Container = styled.p`
  font-weight: 500;
  font-size: 15px;
  color: var(--gray-color);

  width: 100%;
  max-width: 260px;

  margin-top: 15px;
`;

export { Container };
